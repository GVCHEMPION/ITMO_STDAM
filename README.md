# Геометрические фигуры - Проект с CI/CD

Проект для работы с геометрическими фигурами (круг, квадрат, прямоугольник) с автоматическим тестированием через GitHub Actions.

## 📋 Описание

Проект содержит классы для работы с геометрическими фигурами и вычисления их параметров:
- **Circle** (Круг) - вычисление площади и периметра
- **Square** (Квадрат) - вычисление площади и периметра
- **Rectangle** (Прямоугольник) - вычисление площади и периметра

## 🗂️ Структура проекта

```
project/
├── .github/
│   └── workflows/
│       └── main.yml          # CI/CD workflow для автоматического тестирования
├── circle.py                 # Класс Circle
├── square.py                 # Класс Square
├── rectangle.py              # Класс Rectangle
├── test_shapes.py            # Unit-тесты для всех классов
└── README.md                 # Документация проекта
```

## 🚀 Функциональность

### Circle (Круг)
```python
from circle import Circle

circle = Circle(radius=5)
print(circle.area())       # Площадь круга
print(circle.perimeter())  # Длина окружности
```

### Square (Квадрат)
```python
from square import Square

square = Square(side=4)
print(square.area())       # Площадь квадрата
print(square.perimeter())  # Периметр квадрата
```

### Rectangle (Прямоугольник)
```python
from rectangle import Rectangle

rectangle = Rectangle(width=5, height=3)
print(rectangle.area())       # Площадь прямоугольника
print(rectangle.perimeter())  # Периметр прямоугольника
```

## 🧪 Тестирование

Проект использует `unittest` для автоматического тестирования.

### Запуск тестов локально

```bash
# Запуск всех тестов с детальным выводом
python -m unittest test_shapes.py -v

# Или автопоиск и запуск всех тестов
python -m unittest discover -v
```

### Автоматическое тестирование (CI/CD)

При каждом `push` в репозиторий автоматически запускаются тесты на:
- ✅ **Ubuntu Latest**
- ✅ **Windows Latest**

Workflow находится в `.github/workflows/main.yml`

## 📊 Покрытие тестами

Все классы покрыты unit-тестами:
- ✅ Тестирование вычисления площади
- ✅ Тестирование вычисления периметра
- ✅ Проверка на точность вычислений (для круга)

## 🛠️ Технологии

- **Python 3.11**
- **unittest** - фреймворк для тестирования
- **GitHub Actions** - CI/CD автоматизация

## 📦 Установка и использование

1. Клонируйте репозиторий:
```bash
git clone https://github.com/GVCHEMPION/ITMO_STDAM.git
cd ITMO_STDAM
```

2. Проект не требует установки дополнительных зависимостей (используется только стандартная библиотека Python)

3. Запустите тесты:
```bash
python -m unittest test_shapes.py -v
```

## 🔄 CI/CD Pipeline

GitHub Actions автоматически:
1. Клонирует репозиторий
2. Устанавливает Python 3.11
3. Запускает все unit-тесты
4. Выполняет тесты параллельно на Ubuntu и Windows
5. Отображает результаты в разделе Actions

### Просмотр результатов CI/CD

1. Перейдите в репозиторий на GitHubbbbbbb
2. Откройте вкладку **Actions**
3. Выберите последний workflow run
4. Проверьте статус тестов для обеих операционных систем

## 📝 Лабораторная работа №5

Этот проект создан в рамках лабораторной работы по изучению CI/CD с использованием GitHub Actions.

**Цель работы:** Настроить автоматический запуск unit-тестов при каждом push в репозиторий на двух операционных системах (Ubuntu и Windows).
